name: FCAF wallet relying-party DCQL multiple true
runtime:
  fixture:
    verifier_url: https://beta-capture-wallet.credimi.io
  global_device_id: forkbomb-bv-andrea/usb/device
  temporal:
    activity_options:
      schedule_to_close_timeout: 50m
      start_to_close_timeout: 50m
      retry_policy:
        maximum_attempts: 1

steps:
  - id: create-presentation
    use: http-request
    with:
      method: POST
      url: ${fixture.verifier_url}/openid4vp/sessions
      expected_status: 201
      headers:
        Content-Type: application/json
      body:
        scheme: haip-vp://
        request_delivery: by_reference
        response_type: vp_token
        response_mode: direct_post
        presentation_request:
          nonce: fcaf-dcql-multiple-true
          dcql_query:
            credentials:
              - id: pid_sdjwt
                format: dc+sd-jwt
                multiple: true
                meta:
                  vct_values:
                    - urn:eu.europa.ec.eudi:pid:1
                claims:
                  - path:
                      - given_name
                  - path:
                      - family_name

  - id: present-with-reference-implementation
    use: mobile-automation
    with:
      version_id: installed_from_external_source
      parameters:
        DEEPLINK: ${{ create-presentation.outputs.body.deeplink }}
      action_code: |
        appId: eu.europa.ec.euidi
        ---
        - launchApp:
            stopApp: false
            clearState: false
            permissions:
              camera: allow
              notifications: allow
              all: unset
                - openLink: { link: ${env.DEEPLINK}, autoVerify: false, browser: false }
        - waitForAnimationToEnd:
            timeout: 5000
        - takeScreenshot: fcaf-dcql-multiple-true-selection
        - tapOn:
            text: "(?i)(continue|accept|authorize|share|present)"
            optional: true
        - waitForAnimationToEnd:
            timeout: 5000
        - tapOn:
            text: "(?i)(continue|accept|authorize|share|present)"
            optional: true
        - waitForAnimationToEnd:
            timeout: 5000
        - takeScreenshot: fcaf-dcql-multiple-true-submitted

  - id: get-presentation-result
    use: http-request
    with:
      method: GET
      url: ${fixture.verifier_url}/openid4vp/sessions/${{ create-presentation.outputs.body.session_id }}
      expected_status: 200

  - id: run-fcaf-validators
    use: fcaf-validation
    with:
      test_ids:
        - WS_RP_MS_ProtocolMessages__071
      suite: wallet_solution/relying_party
      pipeline_outputs:
        pipeline.dcql.multiple-true:
          output:
            dcql_exchange: ${{ get-presentation-result.outputs.body }}
            visual_evidence: ${{ present-with-reference-implementation.outputs.flow_output.maestro_screenshot_urls }}
            create-presentation: ${{ create-presentation.outputs }}
            present-with-reference-implementation: ${{ present-with-reference-implementation.outputs }}
            get-presentation-result: ${{ get-presentation-result.outputs }}
